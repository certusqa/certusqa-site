# local previews (not deployed)
preview-*.png
.DS_Store

# secrets — never commit
.env
.env.local

# private audit — never publish (repo is deployed to certusqa.com)
docs/AUDIT-*.md

# Cursor rules — keep locally, never deploy (this repo is served at certusqa.com)
.cursor/
