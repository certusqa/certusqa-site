# CertusQA — Marketing Site (certusqa.com)

Static site for **CertusQA**, brand-matched to the CertusQA one-pager (emerald `#10b981` /
navy `#0B1220`, Inter + JetBrains Mono). No framework or package manager; one shell script,
`scripts/build-dist.sh`, assembles the deployable `dist/` from an explicit allowlist (see Deploy).
Repo: [`certusqa/certusqa-site`](https://github.com/certusqa/certusqa-site) (public).

> This repo is the **public marketing site only**. It contains no engine IP — the private engine must never be copied here.

## Structure

```
certusqa-site/
├── index.html                  ← Platform landing → certusqa.com
├── deployshield.html           ← DeployShield Suite services page → /deployshield
├── privacy.html · terms.html   ← Legal pages → /privacy, /terms
├── 404.html
├── proof/                      ← Evidence page → /proof/ (index.html + proof.js)
├── docs/                       ← Product docs → /docs/ (Gate quickstart + real sample outputs)
├── showcase/index.html         ← Interactive 45s engine walkthrough (iframed from /#showcase), storyboard.js
├── showcase/live-ae/           ← Live storefront WebM + captions + sanitised triage JSON + narrative, player.js
├── showcase/evidence/          ← golden-eval.json — GENERATED by qa-forge-core, never hand-edited
├── contract/                   ← Public contract SUT mirror (online deferred proof), contract.js
├── assets/
│   ├── styles.css              ← shared design system
│   ├── fonts.css · fonts/      ← self-hosted Inter, Sora, JetBrains Mono
│   ├── site.js                 ← footer year + mobile nav toggle (every page)
│   ├── cta.js                  ← conversion component (index.html)
│   └── certusqa-icon-mark.png  ← logo mark (nav + favicon)
├── _headers                    ← Cloudflare Pages security headers incl. the CSP
├── _redirects                  ← dotfile guard
├── robots.txt · sitemap.xml
├── scripts/build-dist.sh       ← the deploy allowlist; assembles dist/ (dist/ is gitignored)
├── notes/                      ← local notes and audits — gitignored, never published
└── .github/workflows/leak-gate.yml ← CI: secrets, engine paths, legal pages, inline scripts, allowlist
```

Pages, cross-linked in nav + footer:
- **`/`** — the platform landing. CTAs → Tally intake with attribution (see Conversion component). Includes the `#showcase` iframe walkthrough and the `#live-run` player from `showcase/live-ae/`.
- **`/deployshield`** — the managed DeployShield Suite offer and pricing; CTAs → the same Tally intake.
- **`/proof/`** — the judge benchmark behind every accuracy claim, rendered from `showcase/evidence/golden-eval.json`.
- **`/docs/`** — the CertusQA Gate quickstart, with real `gate.json`, `summary.md` and Proof Artifact samples from the action's own test run.
- **`/showcase/`** — standalone interactive walkthrough (`?embed=1` for clean iframe chrome).
- **`/showcase/live-ae/player`** — captioned live storefront recording (no engine source).
- **`/privacy`, `/terms`** — legal pages; the leak gate fails if either is missing.

Cloudflare Pages serves every `*.html` at its clean URL (`deployshield.html` → `/deployshield`).

## Preview locally

```bash
bash scripts/build-dist.sh && python3 -m http.server 8080 --directory dist   # http://localhost:8080
```

Preview the **built** `dist/`, not the repo root, so what you see is what the allowlist ships.
Two things differ locally: Python's server does not apply `_headers` (so the CSP is not enforced)
and does not resolve clean URLs (`/showcase/live-ae/player` 404s locally; Pages serves it).

Re-render preview screenshots (matches how the site was verified):

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --hide-scrollbars --screenshot=preview-desktop.png --window-size=1280,4200 "file://$PWD/index.html"
```

DNS / email / Pages go-live steps live in the private ops repo.

## Deploy — Cloudflare Pages

Production branch `main`. **Build command `bash scripts/build-dist.sh`, build output directory
`dist`.** The script copies only the directories and files named in its allowlist, prunes
dotfiles and repo furniture, and refuses to build if a required page is missing or a forbidden
path lands in `dist/`. `_headers` and `_redirects` are in that allowlist because Pages reads
them from the output directory; dropping either silently removes the CSP or the dotfile guard.

Until 2026-08-11 the project published the repo root, which served `README.md`, `.gitignore`
and the leak-gate workflow at certusqa.com. If the dashboard ever shows *no build command /
output `/`* again, that regression is back. Confirm the two settings in the Pages project after
any Cloudflare-side change; nothing in this repo can verify them.

## Wiring

- Every CTA on both pages → Tally intake `https://tally.so/r/MePK9X`, with attribution params (below).
  Tally redirects to Calendly after submit; the Stripe link is sent after the kickoff call.
- The **one-pager** button points to `https://certusqa.com`.

## Conversion component (2026-09-06)

Every CTA on `/` goes to the Tally intake `https://tally.so/r/MePK9X` with three query params
appended by `assets/cta.js`: `source` (which block: `home-hero`, `home-footer-band`, `home-nav`,
`home-showcase`, `deployshield-N`), `path` (`demo` | `sandbox` | `design-partner`) and `page`.
**Tally ignores unknown params**, so attribution is empty until you add three Hidden fields named
`source`, `path` and `page` to the form (Tally → form → add block → Hidden fields). No `mailto:`
CTAs remain; the email address stays as a plain contact link.

The "Try the sandbox now" tab fetches `showcase/live-ae/triage-report.json` (same origin, allowed
by `connect-src 'self'`) and renders the artifacts. Without JS the panel still shows the download
and player links.

All inline scripts were extracted to `assets/site.js`, `assets/cta.js`, `showcase/storyboard.js`,
`showcase/live-ae/player.js` and `contract/contract.js`, so the CSP is `script-src 'self'` plus
the Cloudflare Web Analytics origin. Add a new `<script>` as an external file; the leak gate fails
on any unhashed inline script anywhere in the tree.

## Evidence page (`/proof/`) is generated, not written

`showcase/evidence/golden-eval.json` is produced by `qa-forge-core` (`npm run publish:site-evidence`,
and its CI on every merge to `main` once `CERTUSQA_SITE_TOKEN` is set there). Do not hand-edit it;
the next publish would overwrite the edit. `proof/proof.js` renders it at runtime, so every number
on the evidence page traces to that file and to the engine commit stamped inside it.

